extends layouts/main
include mixins/videoBlock

block content
    h3.home-title 최근 업로드된 영상
    .home-videos
        each item in videos
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })